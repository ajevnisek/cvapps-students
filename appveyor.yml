image: Ubuntu1604
environment:
  matrix:
    # For Python versions available on Appveyor, see
    # http://www.appveyor.com/docs/installed-software#python
    - TOXENV: "py38"

build: off

install:
  - pip install tox

test_script:
  - tox -e %TOXENV%
